import { View, Text, ScrollView, Image, TouchableOpacity } from 'react-native';
import { Link } from 'expo-router';

const restaurants = [
  {
    id: '1',
    name: 'Mama Africa',
    city: 'Bruxelles',
    tagline: 'Poulet braisé, attiéké, sauces ivoiriennes, ambiance chaleureuse.',
  },
  {
    id: '2',
    name: 'Lagos Street Food',
    city: 'Bruxelles',
    tagline: 'Jollof rice, suya grillé, street vibes nigérianes.',
  },
  {
    id: '3',
    name: 'Douala Corner',
    city: 'Bruxelles',
    tagline: 'Ndolé, plantain doré, grillades et sonorités camerounaises.',
  },
];

export default function HomeScreen() {
  return (
    <ScrollView
      style={{ flex: 1, backgroundColor: '#0f172a' }}
      contentContainerStyle={{ padding: 24, paddingBottom: 40 }}
    >
      {/* Header logo + titre */}
      <View style={{ alignItems: 'flex-start', marginBottom: 32 }}>
        <Text style={{ color: '#facc15', fontSize: 24, fontWeight: '700' }}>
          DelishAfrica
        </Text>
        <Text
          style={{
            color: '#e5e7eb',
            fontSize: 16,
            marginTop: 4,
            maxWidth: 260,
          }}
        >
          Vos restaurants africains préférés, livrés en temps réel, où vous le voulez.à
          Bruxelles.
        </Text>
      </View>

      {/* Bloc “Next ride / hero” */}
      <View
        style={{
          backgroundColor: '#111827',
          borderRadius: 16,
          padding: 20,
          marginBottom: 24,
          borderWidth: 1,
          borderColor: '#1f2937',
        }}
      >
        <Text
          style={{
            color: '#f9fafb',
            fontSize: 18,
            fontWeight: '600',
            marginBottom: 8,
          }}
        >
          Commande en temps réel
        </Text>
        <Text style={{ color: '#9ca3af', marginBottom: 16 }}>
          Suivez votre commande, du restaurant jusqu&apos;à votre porte, avec
          nos coursiers partenaires.
        </Text>

        <View style={{ flexDirection: 'row', gap: 12 }}>
          <TouchableOpacity
            style={{
              flex: 1,
              backgroundColor: '#facc15',
              borderRadius: 999,
              paddingVertical: 10,
              alignItems: 'center',
            }}
          >
            <Text
              style={{
                color: '#111827',
                fontWeight: '700',
                fontSize: 14,
              }}
            >
              Commencer une commande
            </Text>
          </TouchableOpacity>

          <TouchableOpacity
            style={{
              flex: 1,
              borderRadius: 999,
              borderWidth: 1,
              borderColor: '#4b5563',
              paddingVertical: 10,
              alignItems: 'center',
            }}
          >
            <Text
              style={{
                color: '#e5e7eb',
                fontWeight: '500',
                fontSize: 14,
              }}
            >
              Voir la démo
            </Text>
          </TouchableOpacity>
        </View>
      </View>

      {/* Liste de restos démo */}
      <Text
        style={{
          color: '#f9fafb',
          fontSize: 16,
          fontWeight: '600',
          marginBottom: 12,
        }}
      >
        Sélection DelishAfrica – Bruxelles (démo) (démo)
      </Text>

      {restaurants.map((r) => (
        <View
          key={r.id}
          style={{
            backgroundColor: '#020617',
            borderRadius: 14,
            padding: 16,
            marginBottom: 12,
            borderWidth: 1,
            borderColor: '#111827',
          }}
        >
          <Text
            style={{
              color: '#f9fafb',
              fontSize: 15,
              fontWeight: '600',
              marginBottom: 2,
            }}
          >
            {r.name}
          </Text>
          <Text style={{ color: '#9ca3af', fontSize: 13 }}>{r.city}</Text>
          <Text style={{ color: '#e5e7eb', fontSize: 13, marginTop: 6 }}>
            {r.tagline}
          </Text>
        </View>
      ))}

      {/* Lien vers le reste de l’app (démo) */}
      <View style={{ marginTop: 24 }}>
        <Link href="/profil" asChild>
          <TouchableOpacity
            style={{
              paddingVertical: 10,
              borderRadius: 999,
              borderWidth: 1,
              borderColor: '#4b5563',
              alignItems: 'center',
            }}
          >
            <Text style={{ color: '#e5e7eb', fontSize: 13 }}>
              Voir un exemple de commande en temps réel
            </Text>
          </TouchableOpacity>
        </Link>
      </View>
    </ScrollView>
  );
}
