import React from "react";

/**
 * DemoFab TEMP DISABLED
 * Reason: suspected global touch capture (absolute + zIndex).
 * Re-enable after scroll is confirmed OK.
 */
export default function DemoFab() {
  return null;
}
