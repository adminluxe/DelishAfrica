import React from 'react';
import { ScrollView, View, Text, Pressable } from 'react-native';
import { useRouter } from 'expo-router';
import { demoMissions } from './demoMissions';

export default function CourierHome() {
  const router = useRouter();

  return (
    <ScrollView
      style={{ flex: 1, backgroundColor: '#020617' }}
      contentContainerStyle={{ padding: 24, paddingBottom: 40 }}
    >
      {/* Titre */}
      <Text
        style={{
          fontSize: 28,
          fontWeight: '700',
          color: '#FACC15',
          textAlign: 'center',
          marginBottom: 8,
        }}
      >
        DelishAfrica – Coursier
      </Text>
      <Text
        style={{
          fontSize: 16,
          color: '#E5E7EB',
          textAlign: 'center',
        }}
      >
        Vue démo des missions en temps réel sur ta tournée.
      </Text>
      <Text
        style={{
          fontSize: 14,
          color: '#9CA3AF',
          textAlign: 'center',
          marginBottom: 24,
        }}
      >
        Simule les missions envoyées depuis les apps Client et Marchand.
      </Text>

      {/* Bloc état global */}
      <View
        style={{
          backgroundColor: '#020617',
          borderRadius: 24,
          padding: 20,
          borderWidth: 1,
          borderColor: '#1E293B',
          marginBottom: 24,
        }}
      >
        <View
          style={{
            flexDirection: 'row',
            alignItems: 'center',
            marginBottom: 8,
          }}
        >
          <View
            style={{
              width: 10,
              height: 10,
              borderRadius: 999,
              backgroundColor: '#22C55E',
              marginRight: 8,
            }}
          />
          <Text
            style={{
              fontSize: 16,
              fontWeight: '600',
              color: '#22C55E',
            }}
          >
            En ligne (démo)
          </Text>
        </View>
        <Text style={{ fontSize: 14, color: '#CBD5F5' }}>
          Exemple de tournée active, avec missions à récupérer et livrer.
        </Text>
      </View>

      {/* Liste des missions */}
      <Text
        style={{
          fontSize: 18,
          fontWeight: '700',
          color: '#F9FAFB',
          marginBottom: 8,
        }}
      >
        Missions disponibles (démo)
      </Text>
      <Text
        style={{
          fontSize: 14,
          color: '#9CA3AF',
          marginBottom: 16,
        }}
      >
        Chaque carte représente une mission. Tape dessus pour voir le détail.
      </Text>

      {demoMissions.map((mission) => (
        <Pressable
          key={mission.id}
          onPress={() => router.push(`/mission/${mission.id}`)}
          style={({ pressed }) => ({
            backgroundColor: '#020617',
            borderRadius: 24,
            padding: 20,
            borderWidth: 1,
            borderColor: pressed ? '#FACC15' : '#1E293B',
            marginBottom: 14,
          })}
        >
          <Text
            style={{
              fontSize: 16,
              fontWeight: '600',
              color: '#F9FAFB',
              marginBottom: 4,
            }}
          >
            {mission.title}
          </Text>
          <Text
            style={{
              fontSize: 14,
              color: '#9CA3AF',
              marginBottom: 2,
            }}
          >
            Client : {mission.client}
          </Text>
          <Text
            style={{ fontSize: 14, color: '#9CA3AF', marginBottom: 4 }}
          >
            Adresse : {mission.address}
          </Text>
          <View
            style={{
              flexDirection: 'row',
              justifyContent: 'space-between',
              marginTop: 4,
            }}
          >
            <Text style={{ fontSize: 14, color: '#E5E7EB' }}>
              ETA : {mission.eta}
            </Text>
            <Text
              style={{
                fontSize: 16,
                fontWeight: '700',
                color: '#FACC15',
              }}
            >
              {mission.price}
            </Text>
          </View>
        </Pressable>
      ))}
    </ScrollView>
  );
}
